name: Docker Image CI

on:
  push:
    branches: [ "001-pahr-textbook-rag" ]
  pull_request:
    branches: [ "001-pahr-textbook-rag" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
